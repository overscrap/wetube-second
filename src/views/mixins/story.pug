mixin story(param)
    a(href=`/stories/${param.id}`).video-mixin
        div.video-mixin__thumb
        div.video-mixin__data
            span.video-mixin__title=param.title
            div.video-mixin__meta
                span ownername • 
                span #{param.meta.views} 회